# Copie para .env e ajuste. O .env NÃO vai pro git.
POSTGRES_USER=deusold
POSTGRES_PASSWORD=troque-esta-senha
POSTGRES_DB=deusold

# Porta pública do dashboard no host (VPS)
DASHBOARD_PORT=8501

# Coletor
FREQUENT_MINUTES=15
WORLD_FILTER=1
